# Shopify Variant Price Manager

This Flask app allows you to manage variant surcharges for bracelets and colliers in your Shopify store.

## Setup

1. Install dependencies:
   ```bash
   pip install flask requests
